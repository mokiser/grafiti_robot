@startuml
actor "Пользователь" as User
actor "Камера" as Camera
actor "Сервер" as Server

User --> (Увидеть граффити)
Server --> Camera: Запросить сделать фото
Camera --> (Сделать фото)
Camera --> (Вызвать робота)

(Увидеть граффити) --> (Сообщить системе)
(Сообщить системе) --> Server: Сообщить о проблеме
(Вызвать робота) --> (Подтвердить вызов)


Server --> (Считать изображение с камеры)
Server --> (Распознать робота)
Server --> (Рассчитать путь)
Server --> (Отправить команду движения)
Server --> (Избегать препятствий) : при движении

(Распознать робота) --> (Направлять робота)
(Направлять робота) --> (Избегать препятствий)

@enduml
